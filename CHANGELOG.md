# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added

- `python -m limenex validate [path]` CLI command that loads a policies YAML file via `LocalFilePolicyStore` and reports skill and policy counts, or a clear error message pointing to the problem. Defaults to `.limenex/policies.yaml`. (#2)

### Changed

### Fixed
